class Review{
  String id;
  String tutorId;
  String studentId;
  int rating;
  String title;
  String comments;
  Review(this.id, this.tutorId, this.studentId, this.rating, this.title, this.comments);
}
